# Expected transition

The user may briefly see a neutral `Loading Binder…` / `Preparing Binder…` state while the selected Binder is resolved. The UI must never paint inventory belonging to a different Binder and then remove it afterward.
