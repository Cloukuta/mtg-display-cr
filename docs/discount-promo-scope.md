# Scope

This hotfix is presentation-only. It consumes `pricingMode`, `referenceCrc`, `marketPriceUsd`, and `discountPercent` already passed to `VisualPrice`. No Supabase schema or resolver calculation is changed.
