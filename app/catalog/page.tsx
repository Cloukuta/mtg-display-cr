"use client";

import { useEffect, useState } from "react";
import { ArrowRight, Box, Layers3 } from "lucide-react";
import AppHeader from "@/components/AppHeader";
import { DEFAULT_LANGUAGE, LANGUAGE_STORAGE_KEY, type Language } from "@/lib/i18n";

export default function CatalogPage(){
 const[language,setLanguage]=useState<Language>(DEFAULT_LANGUAGE);const es=language==="es";
 useEffect(()=>{const stored=localStorage.getItem(LANGUAGE_STORAGE_KEY);if(stored==="es"||stored==="en")setLanguage(stored);const h=(e:Event)=>{const v=(e as CustomEvent<Language>).detail;if(v==="es"||v==="en")setLanguage(v)};window.addEventListener("mtg-language-change",h);return()=>window.removeEventListener("mtg-language-change",h)},[]);
 return <main className="min-h-screen bg-background text-foreground"><AppHeader currentPath="/catalog"/><div className="mx-auto max-w-7xl px-5 py-8"><p className="text-xs font-bold uppercase tracking-[.15em] text-primary">{es?"INVENTARIO DEL VENDEDOR":"SELLER INVENTORY"}</p><h1 className="mt-2 font-serif text-4xl">{es?"Tu catálogo":"Your Catalog"}</h1><p className="mt-2 max-w-2xl text-muted-foreground">{es?"Administra los diferentes tipos de productos disponibles en tu tienda.":"Manage the different product types available in your store."}</p><div className="mt-8 grid gap-5 md:grid-cols-2"><a href="/catalog/singles" className="group rounded-3xl border border-border bg-card p-6 transition hover:border-primary/50"><div className="flex items-start justify-between gap-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-primary/10 text-primary"><Layers3/></div><ArrowRight className="text-muted-foreground transition group-hover:translate-x-1 group-hover:text-primary"/></div><h2 className="mt-6 font-serif text-3xl">Singles</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{es?"Administra tus cartas individuales y organízalas por Binders.":"Manage individual cards and organize them by Binders."}</p><span className="mt-5 inline-flex text-sm font-bold text-primary">{es?"Administrar Singles":"Manage Singles"} →</span></a><section className="rounded-3xl border border-border bg-card p-6 opacity-70"><div className="flex items-start justify-between gap-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-secondary text-muted-foreground"><Box/></div><span className="rounded-full border border-border px-3 py-1 text-xs font-bold text-muted-foreground">{es?"PRÓXIMAMENTE":"COMING SOON"}</span></div><h2 className="mt-6 font-serif text-3xl">{es?"Producto sellado":"Sealed Products"}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{es?"Booster boxes, bundles, Commander decks y otros productos sellados tendrán su propio inventario.":"Booster boxes, bundles, Commander decks and other sealed products will have their own inventory."}</p></section></div></div></main>
}
