"use client";
import {useEffect,useMemo,useState} from "react";
import {ArrowLeft,ImagePlus,MapPin,MessageCircle,Send,Minus,Plus,TriangleAlert,X,CheckCircle2,Clock3} from "lucide-react";
import {useParams} from "next/navigation";
import BrandLogo from "@/components/BrandLogo";
import {getSupabase} from "@/lib/supabase";
import {DEFAULT_LANGUAGE,LANGUAGE_STORAGE_KEY,type Language} from "@/lib/i18n";

type Item={id:number;quantity:number;requested_quantity:number;seller_confirmed_quantity:number|null;unit_price_crc:number;finish:string;condition:string;language:string;inventory_items:{cards:{name:string;set_code:string;collector_number:string;image_uri:string|null}|null}|null};
type Message={id:number;sender_id:string;message:string;created_at:string};
type Attachment={id:number;uploaded_by:string;attachment_type:string;storage_path:string;mime_type:string;size_bytes:number;created_at:string};
type Event={id:number;actor_id:string|null;actor_role:string;event_type:string;message:string|null;created_at:string};
type Order={id:number;buyer_id:string;seller_id:string;status:string;created_at:string;customer_name:string|null;customer_note:string|null;delivery_store_name:string|null;delivery_location_name:string|null;subtotal_crc:number|null;shipping_fee_crc:number|null;total_crc:number|null;inventory_changes_pending:boolean;response_required_from:string|null;response_required_at:string|null;cancellation_reason:string|null;cancellation_note:string|null;profiles:{public_name:string|null;whatsapp:string|null}|null;order_items:Item[]};
const crc=new Intl.NumberFormat("es-CR",{style:"currency",currency:"CRC",maximumFractionDigits:0});
const steps=["inventory_confirmation","payment_pending","paid","preparing_shipment","shipped","completed"];
const labels={es:{inventory_confirmation:"Confirmando inventario",payment_pending:"Esperando pago",paid:"Pago confirmado",preparing_shipment:"Preparando envío",shipped:"Enviado",completed:"Completado",cancelled:"Cancelado"},en:{inventory_confirmation:"Confirming inventory",payment_pending:"Waiting for payment",paid:"Payment confirmed",preparing_shipment:"Preparing shipment",shipped:"Shipped",completed:"Completed",cancelled:"Cancelled"}} as const;
const reasonLabels:Record<string,{es:string;en:string}>={insufficient_inventory:{es:"No tenía la cantidad deseada",en:"Requested quantity unavailable"},buyer_changed_mind:{es:"Cambié de opinión",en:"I changed my mind"},changes_not_agreed:{es:"No se pudo acordar un cambio en el pedido",en:"Order changes could not be agreed"},seller_no_response_24h:{es:"Vendedor no responde en 24 horas",en:"Seller has not responded in 24 hours"},seller_no_response_48h:{es:"Vendedor no responde en 48 horas",en:"Seller has not responded in 48 hours"},buyer_no_response_24h:{es:"Cliente no responde en 24 horas",en:"Buyer has not responded in 24 hours"},buyer_no_response_48h:{es:"Cliente no responde en 48 horas",en:"Buyer has not responded in 48 hours"},other:{es:"Otro",en:"Other"}};
export default function OrderRoom(){
 const{id}=useParams<{id:string}>();
 const[lang,setLang]=useState<Language>(DEFAULT_LANGUAGE),[uid,setUid]=useState(""),[order,setOrder]=useState<Order|null>(null),[messages,setMessages]=useState<Message[]>([]),[attachments,setAttachments]=useState<Attachment[]>([]),[events,setEvents]=useState<Event[]>([]),[draft,setDraft]=useState(""),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[uploading,setUploading]=useState(false),[notice,setNotice]=useState(""),[quantities,setQuantities]=useState<Record<number,number>>({}),[cancelOpen,setCancelOpen]=useState(false),[cancelReason,setCancelReason]=useState(""),[cancelNote,setCancelNote]=useState("");
 const es=lang==="es",closed=order?.status==="completed"||order?.status==="cancelled",isSeller=!!order&&uid===order.seller_id,isBuyer=!!order&&uid===order.buyer_id;
 useEffect(()=>{const v=localStorage.getItem(LANGUAGE_STORAGE_KEY);if(v==="es"||v==="en")setLang(v);void load()},[id]);
 async function load(){const s=getSupabase();if(!s){setLoading(false);return}const{data:{user}}=await s.auth.getUser();if(!user){location.href="/";return}setUid(user.id);const{data:o,error}=await s.from("orders").select("id,buyer_id,seller_id,status,created_at,customer_name,customer_note,delivery_store_name,delivery_location_name,subtotal_crc,shipping_fee_crc,total_crc,inventory_changes_pending,response_required_from,response_required_at,cancellation_reason,cancellation_note,profiles!orders_seller_id_fkey(public_name,whatsapp),order_items(id,quantity,requested_quantity,seller_confirmed_quantity,unit_price_crc,finish,condition,language,inventory_items(cards(name,set_code,collector_number,image_uri)))").eq("id",Number(id)).maybeSingle();if(error||!o){setNotice(es?"No encontramos este pedido o no tienes acceso.":"We couldn't find this order or you don't have access.");setLoading(false);return}const parsed=o as unknown as Order;if(user.id!==parsed.buyer_id&&user.id!==parsed.seller_id){setNotice(es?"No tienes acceso a este pedido.":"You don't have access to this order.");setLoading(false);return}setOrder(parsed);setQuantities(Object.fromEntries(parsed.order_items.map(i=>[i.id,i.seller_confirmed_quantity??i.quantity])));const[{data:m},{data:a},{data:e}]=await Promise.all([s.from("order_messages").select("id,sender_id,message,created_at").eq("order_id",Number(id)).order("created_at"),s.from("order_attachments").select("id,uploaded_by,attachment_type,storage_path,mime_type,size_bytes,created_at").eq("order_id",Number(id)).order("created_at"),s.from("order_events").select("id,actor_id,actor_role,event_type,message,created_at").eq("order_id",Number(id)).order("created_at")]);setMessages((m||[]) as Message[]);setAttachments((a||[]) as Attachment[]);setEvents((e||[]) as Event[]);setLoading(false)}
 function friendlyError(message:string){console.error("Order action failed",{orderId:order?.id,message});if(message.includes("ORDER_HAS_NO_ITEMS"))return es?"No puedes confirmar un pedido con todas las cartas en 0. Cancela el pedido indicando el motivo.":"You cannot confirm an order with every card at 0. Cancel the order and provide a reason.";if(message.includes("CANCELLATION_REASON_NOT_AVAILABLE"))return es?"Ese motivo de cancelación todavía no está disponible.":"That cancellation reason is not available yet.";if(message.includes("PAYMENT_PROOF_REQUIRED"))return es?"El comprador todavía no ha adjuntado el comprobante de pago.":"The buyer has not attached the payment proof yet.";if(message.includes("SHIPMENT_PROOF_REQUIRED"))return es?"Debes adjuntar una foto o comprobante del envío antes de marcar el pedido como enviado.":"Attach shipment proof before marking the order as shipped.";if(message.includes("INVALID_ORDER_ACTION"))return es?"Esta acción ya no está disponible para el estado actual del pedido. Actualiza la página e inténtalo nuevamente.":"This action is no longer available for the current order status. Refresh and try again.";return es?"No pudimos completar la acción. Inténtalo de nuevo.":"We couldn't complete the action. Please try again."}
 function changeQuantity(item:Item,delta:number){setQuantities(current=>{const value=current[item.id]??item.quantity;return{...current,[item.id]:Math.min(item.requested_quantity,Math.max(0,value+delta))}})}
 async function proposeInventory(){if(!order||!isSeller)return;const remaining=order.order_items.reduce((n,i)=>n+(quantities[i.id]??i.quantity),0);if(!remaining){setNotice(es?"Todas las cantidades están en 0. Debes cancelar el pedido.":"All quantities are 0. You must cancel the order.");return}const s=getSupabase();if(!s)return;setBusy(true);setNotice("");const items=order.order_items.map(i=>({order_item_id:i.id,quantity:quantities[i.id]??i.quantity}));const{error}=await s.rpc("propose_inventory_confirmation",{p_order_id:order.id,p_items:items});if(error)setNotice(friendlyError(error.message));else await load();setBusy(false)}
 async function respondChanges(accept:boolean){if(!order||!isBuyer)return;const s=getSupabase();if(!s)return;setBusy(true);setNotice("");const{error}=await s.rpc("respond_inventory_changes",{p_order_id:order.id,p_accept:accept});if(error)setNotice(friendlyError(error.message));else await load();setBusy(false)}
 async function advance(action:string){if(!order)return;const s=getSupabase();if(!s)return;setBusy(true);setNotice("");const{error}=await s.rpc("advance_order_status",{p_order_id:order.id,p_action:action});if(error)setNotice(friendlyError(error.message));else await load();setBusy(false)}
 async function cancelOrder(){if(!order||!cancelReason)return;const s=getSupabase();if(!s)return;setBusy(true);setNotice("");const{error}=await s.rpc("cancel_order_with_reason",{p_order_id:order.id,p_reason:cancelReason,p_note:cancelNote||null});if(error)setNotice(friendlyError(error.message));else{setCancelOpen(false);await load()}setBusy(false)}
 async function send(){const text=draft.trim();if(!text||closed)return;const s=getSupabase();if(!s)return;setBusy(true);const{data,error}=await s.from("order_messages").insert({order_id:Number(id),sender_id:uid,message:text}).select("id,sender_id,message,created_at").single();if(error)setNotice(es?"No pudimos enviar el mensaje.":"We couldn't send the message.");else{setMessages(x=>[...x,data as Message]);setDraft("")}setBusy(false)}
 async function upload(file:File|null){if(!file||closed)return;if(!["image/jpeg","image/png","image/webp"].includes(file.type)||file.size>5242880){setNotice(es?"Usa una imagen JPG, PNG o WebP de máximo 5 MB.":"Use a JPG, PNG or WebP image up to 5 MB.");return}const s=getSupabase();if(!s)return;setUploading(true);setNotice("");const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,"-");const path=`${id}/${uid}/${crypto.randomUUID()}-${safe}`;const{error:up}=await s.storage.from("order-attachments").upload(path,file,{contentType:file.type,upsert:false});if(up){setNotice(es?"No pudimos subir la imagen.":"We couldn't upload the image.");setUploading(false);return}const type=order?.status==="payment_pending"&&isBuyer?"payment_proof":(order?.status==="preparing_shipment"||order?.status==="shipped")&&isSeller?"shipment_proof":"other";const{data,error}=await s.from("order_attachments").insert({order_id:Number(id),uploaded_by:uid,attachment_type:type,storage_path:path,mime_type:file.type,size_bytes:file.size}).select("id,uploaded_by,attachment_type,storage_path,mime_type,size_bytes,created_at").single();if(error){await s.storage.from("order-attachments").remove([path]);setNotice(es?"No pudimos registrar el archivo.":"We couldn't register the file.")}else setAttachments(x=>[...x,data as Attachment]);setUploading(false)}
 async function openAttachment(a:Attachment){const s=getSupabase();if(!s)return;const{data,error}=await s.storage.from("order-attachments").createSignedUrl(a.storage_path,60);if(error||!data?.signedUrl){setNotice(es?"No pudimos abrir la imagen.":"We couldn't open the image.");return}window.open(data.signedUrl,"_blank","noopener,noreferrer")}
 const proposedSubtotal=order?.order_items.reduce((n,i)=>n+Number(i.unit_price_crc)*(quantities[i.id]??i.quantity),0)??0,shipping=Number(order?.shipping_fee_crc??0),subtotal=Number(order?.subtotal_crc??proposedSubtotal),total=Number(order?.total_crc??subtotal+shipping),hasChanges=!!order&&order.order_items.some(i=>(quantities[i.id]??i.quantity)!==i.requested_quantity),current=order?steps.indexOf(order.status):-1;
 const activity=useMemo(()=>[...messages.map(m=>({kind:"message" as const,date:m.created_at,value:m})),...attachments.map(a=>({kind:"attachment" as const,date:a.created_at,value:a})),...events.map(e=>({kind:"event" as const,date:e.created_at,value:e}))].sort((a,b)=>new Date(a.date).getTime()-new Date(b.date).getTime()),[messages,attachments,events]);
 const paymentProof=attachments.find(a=>a.attachment_type==="payment_proof"),shipmentProof=attachments.find(a=>a.attachment_type==="shipment_proof");
 const elapsedHours=order?.response_required_at?(Date.now()-new Date(order.response_required_at).getTime())/3600000:0;
 const cancellationOptions=useMemo(()=>{if(!order)return[];const base=isSeller?["insufficient_inventory","changes_not_agreed","other"]:["buyer_changed_mind","changes_not_agreed","other"];const waitingForOther=(isSeller&&order.response_required_from==="buyer")||(isBuyer&&order.response_required_from==="seller");const r=[...base];if(waitingForOther&&elapsedHours>=24)r.push(isSeller?"buyer_no_response_24h":"seller_no_response_24h");if(waitingForOther&&elapsedHours>=48)r.push(isSeller?"buyer_no_response_48h":"seller_no_response_48h");return r},[order,isSeller,isBuyer,elapsedHours]);
 if(loading)return <main className="grid min-h-screen place-items-center bg-background text-muted-foreground">{es?"Cargando pedido…":"Loading order…"}</main>;
 if(!order)return <main className="grid min-h-screen place-items-center bg-background p-5 text-foreground"><div className="text-center"><p>{notice}</p><a href="/orders" className="mt-4 inline-flex rounded-xl bg-primary px-5 py-3 font-bold text-primary-foreground">{es?"Volver":"Back"}</a></div></main>;
 const counterpart=isSeller?(order.customer_name||(es?"Comprador":"Buyer")):(order.profiles?.public_name||(es?"Vendedor":"Seller")),back=isSeller?"/pending-sales":"/orders",waitingForMe=order.response_required_from===(isSeller?"seller":"buyer");
 return <main className="min-h-screen bg-background text-foreground"><header className="border-b border-border"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4"><BrandLogo href="/"/><a href={back} className="flex items-center gap-2 text-sm font-semibold text-muted-foreground"><ArrowLeft size={16}/>{isSeller?(es?"Ventas pendientes":"Pending sales"):(es?"Mis pedidos":"My orders")}</a></div></header><div className="mx-auto grid max-w-6xl gap-6 px-4 py-8 lg:grid-cols-[1fr_390px]"><section><p className="text-xs font-black uppercase tracking-[.16em] text-primary">{es?"Pedido":"Order"} #{order.id}</p><div className="mt-2 flex flex-wrap items-end justify-between gap-3"><div><h1 className="font-serif text-4xl">{counterpart}</h1><p className="mt-1 text-sm text-muted-foreground">{new Date(order.created_at).toLocaleString(es?"es-CR":"en-US")}</p></div><span className="rounded-full bg-primary/10 px-3 py-1.5 text-sm font-bold text-primary">{labels[lang][order.status as keyof typeof labels.es]||order.status}</span></div><div className="mt-6 grid grid-cols-6 gap-1">{steps.map((s,i)=><div key={s} className={`h-2 rounded-full ${i<=current&&current>=0?"bg-primary":"bg-secondary"}`}/>)}</div>
 {order.inventory_changes_pending&&<div className="mt-5 rounded-2xl border border-amber-500/30 bg-amber-500/10 p-4"><div className="flex gap-3"><TriangleAlert className="shrink-0 text-amber-600" size={20}/><div><strong>{es?"Cambios de inventario pendientes":"Inventory changes pending"}</strong><p className="mt-1 text-sm text-muted-foreground">{waitingForMe?(es?"Se requiere tu respuesta para continuar el pedido.":"Your response is required to continue the order."):(es?"Esperando respuesta de la otra persona.":"Waiting for the other person's response.")}</p></div></div></div>}
 {order.status==="payment_pending"&&<div className={`mt-5 rounded-2xl border p-4 ${paymentProof?"border-emerald-500/30 bg-emerald-500/10":"border-amber-500/30 bg-amber-500/10"}`}><div className="flex gap-3">{paymentProof?<CheckCircle2 className="shrink-0 text-emerald-500" size={20}/>:<Clock3 className="shrink-0 text-amber-500" size={20}/>}<div className="flex-1"><strong>{paymentProof?(es?"Comprobante de pago enviado":"Payment proof submitted"):(isBuyer?(es?"Pago pendiente":"Payment pending"):(es?"Esperando comprobante del comprador":"Waiting for buyer payment proof"))}</strong><p className="mt-1 text-sm text-muted-foreground">{paymentProof?(isSeller?(es?"Revisa el comprobante antes de confirmar que recibiste el pago.":"Review the proof before confirming payment received."):(es?"El vendedor debe revisar y confirmar tu pago.":"The seller must review and confirm your payment.")):(isBuyer?(es?"Realiza el SINPE y adjunta aquí una captura del comprobante.":"Send the SINPE payment and attach the payment proof here."):(es?"El botón de confirmar pago se habilitará cuando el comprador adjunte su comprobante.":"Confirm payment will unlock after the buyer attaches proof."))}</p>{paymentProof&&<button type="button" onClick={()=>void openAttachment(paymentProof)} className="mt-3 rounded-xl border border-border px-3 py-2 text-xs font-bold">{es?"Ver comprobante":"View proof"}</button>}</div></div></div>}
 {order.status==="preparing_shipment"&&isSeller&&<div className={`mt-5 rounded-2xl border p-4 ${shipmentProof?"border-emerald-500/30 bg-emerald-500/10":"border-amber-500/30 bg-amber-500/10"}`}><strong>{shipmentProof?(es?"Evidencia de envío adjunta":"Shipment proof attached"):(es?"Adjunta evidencia del envío":"Attach shipment proof")}</strong><p className="mt-1 text-sm text-muted-foreground">{shipmentProof?(es?"Ya puedes marcar el pedido como enviado.":"You can now mark the order as shipped."):(es?"Sube una foto del sobre o comprobante antes de marcar el pedido como enviado.":"Upload a photo of the package or shipping receipt before marking it shipped.")}</p></div>}
 <div className="mt-6 rounded-3xl border border-border bg-card"><div className="border-b border-border p-5"><h2 className="font-serif text-2xl">{es?"Cartas":"Cards"}</h2>{isSeller&&order.status==="inventory_confirmation"&&!order.inventory_changes_pending&&<p className="mt-1 text-xs text-muted-foreground">{es?"Ajusta la cantidad disponible. 0 elimina la carta de esta propuesta.":"Adjust available quantity. 0 removes the card from this proposal."}</p>}</div><div className="divide-y divide-border">{order.order_items.map(i=>{const c=i.inventory_items?.cards,q=quantities[i.id]??i.quantity,proposal=order.inventory_changes_pending?(i.seller_confirmed_quantity??i.quantity):q,removed=proposal===0;return <div key={i.id} className={`flex gap-4 p-5 ${removed?"opacity-50":""}`}>{c?.image_uri?<img src={c.image_uri} alt={c.name} className="h-24 w-16 rounded-lg object-cover"/>:<div className="h-24 w-16 rounded-lg bg-secondary"/>}<div className="min-w-0 flex-1"><strong className={removed?"line-through":""}>{c?.name||"Card"}</strong><p className="text-xs text-muted-foreground">{c?.set_code?.toUpperCase()} #{c?.collector_number} · {i.condition} · {i.finish} · {i.language}</p>{isSeller&&order.status==="inventory_confirmation"&&!order.inventory_changes_pending?<div className="mt-3 flex items-center gap-3"><button type="button" onClick={()=>changeQuantity(i,-1)} disabled={q<=0} className="grid h-8 w-8 place-items-center rounded-lg border border-border disabled:opacity-30"><Minus size={14}/></button><strong>{q} {es?"de":"of"} {i.requested_quantity}</strong><button type="button" onClick={()=>changeQuantity(i,1)} disabled={q>=i.requested_quantity} className="grid h-8 w-8 place-items-center rounded-lg border border-border disabled:opacity-30"><Plus size={14}/></button>{q===0&&<span className="text-xs font-bold text-destructive">{es?"Se eliminará":"Will be removed"}</span>}</div>:<p className="mt-2 text-sm">{proposal} × {crc.format(Number(i.unit_price_crc))}{proposal!==i.requested_quantity&&<span className="ml-2 text-xs text-muted-foreground">({es?`solicitó ${i.requested_quantity}`:`requested ${i.requested_quantity}`})</span>}</p>}</div><strong className="text-primary">{crc.format(Number(i.unit_price_crc)*proposal)}</strong></div>})}</div></div>
 {isSeller&&order.status==="inventory_confirmation"&&!order.inventory_changes_pending&&<div className="mt-5 rounded-3xl border border-border bg-card p-5"><h3 className="font-semibold">{hasChanges?(es?"Resumen de cambios":"Change summary"):(es?"Confirmación de inventario":"Inventory confirmation")}</h3><div className="mt-3 flex justify-between text-sm"><span>{es?"Nuevo subtotal":"New subtotal"}</span><strong>{crc.format(proposedSubtotal)}</strong></div><div className="mt-1 flex justify-between text-sm"><span>{es?"Envío":"Shipping"}</span><strong>{crc.format(shipping)}</strong></div><div className="mt-3 flex justify-between border-t border-border pt-3"><strong>{es?"Nuevo total":"New total"}</strong><strong className="text-primary">{crc.format(proposedSubtotal+shipping)}</strong></div><button onClick={()=>void proposeInventory()} disabled={busy||proposedSubtotal<=0} className="mt-4 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground disabled:opacity-40">{busy?(es?"Guardando…":"Saving…"):hasChanges?(es?"Enviar cambios al comprador":"Send changes to buyer"):(es?"Confirmar inventario":"Confirm inventory")}</button></div>}
 {isBuyer&&order.inventory_changes_pending&&order.response_required_from==="buyer"&&<div className="mt-5 rounded-3xl border border-primary/30 bg-primary/5 p-5"><h3 className="font-serif text-2xl">{es?"El vendedor propuso cambios":"Seller proposed changes"}</h3><p className="mt-2 text-sm text-muted-foreground">{es?"Revisa las cantidades y el nuevo total. El pago solo se habilitará si aceptas.":"Review quantities and the new total. Payment is enabled only if you accept."}</p><div className="mt-4 flex gap-3"><button onClick={()=>void respondChanges(false)} disabled={busy} className="flex-1 rounded-xl border border-border px-4 py-3 font-bold">{es?"No aceptar":"Decline"}</button><button onClick={()=>void respondChanges(true)} disabled={busy} className="flex-1 rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">{es?"Aceptar cambios":"Accept changes"}</button></div></div>}
 <div className="mt-5 rounded-3xl border border-border bg-card p-5"><div className="flex items-start gap-3"><MapPin className="mt-1 text-primary" size={20}/><div><h2 className="font-semibold">{es?"Punto de retiro":"Pickup location"}</h2><p className="text-muted-foreground">{order.delivery_store_name} · {order.delivery_location_name}</p></div></div></div></section>
 <aside className="space-y-5"><section className="rounded-3xl border border-border bg-card p-5"><h2 className="font-serif text-2xl">{es?"Resumen":"Summary"}</h2><div className="mt-4 space-y-2 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">{es?"Cartas":"Cards"}</span><span>{crc.format(order.status==="inventory_confirmation"&&isSeller&&!order.inventory_changes_pending?proposedSubtotal:subtotal)}</span></div><div className="flex justify-between"><span className="text-muted-foreground">{es?"Envío":"Shipping"}</span><span>{crc.format(shipping)}</span></div><div className="flex justify-between border-t border-border pt-3 text-lg"><strong>Total</strong><strong className="font-serif text-2xl text-primary">{crc.format(order.status==="inventory_confirmation"&&isSeller&&!order.inventory_changes_pending?proposedSubtotal+shipping:total)}</strong></div></div>
 {isSeller&&order.status==="payment_pending"&&<button onClick={()=>void advance("confirm_payment")} disabled={busy||!paymentProof} title={!paymentProof?(es?"Esperando comprobante del comprador":"Waiting for buyer payment proof"):undefined} className="mt-4 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40">{paymentProof?(es?"Confirmar pago recibido":"Confirm payment received"):(es?"Esperando comprobante":"Waiting for proof")}</button>}{isSeller&&order.status==="paid"&&<button onClick={()=>void advance("start_preparing")} disabled={busy} className="mt-4 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">{es?"Preparar pedido":"Prepare order"}</button>}{isSeller&&order.status==="preparing_shipment"&&<button onClick={()=>void advance("mark_shipped")} disabled={busy||!shipmentProof} className="mt-4 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-40">{shipmentProof?(es?"Marcar enviado":"Mark shipped"):(es?"Adjunta evidencia para enviar":"Attach proof to ship")}</button>}{isBuyer&&order.status==="shipped"&&<button onClick={()=>void advance("confirm_received")} disabled={busy} className="mt-4 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">{es?"Confirmar recibido":"Confirm received"}</button>}
 {!closed&&!["paid","preparing_shipment","shipped"].includes(order.status)&&<button onClick={()=>{setCancelReason("");setCancelNote("");setCancelOpen(true)}} className="mt-3 w-full rounded-xl border border-destructive/40 px-4 py-2 text-sm font-bold text-destructive">{es?"Cancelar pedido":"Cancel order"}</button>}</section>
 <section className="overflow-hidden rounded-3xl border border-border bg-card"><div className="border-b border-border p-5"><div className="flex items-center gap-2"><MessageCircle className="text-primary" size={20}/><h2 className="font-serif text-2xl">{es?"Actividad y conversación":"Activity & conversation"}</h2></div></div><div className="max-h-[460px] space-y-3 overflow-y-auto p-4">{activity.length===0&&<p className="py-8 text-center text-sm text-muted-foreground">{es?"Aún no hay actividad.":"No activity yet."}</p>}{activity.map(x=>x.kind==="message"?<div key={`m${x.value.id}`} className={`rounded-2xl p-3 text-sm ${x.value.sender_id===uid?"ml-8 bg-primary/10":"mr-8 bg-secondary"}`}><p>{x.value.message}</p><p className="mt-1 text-[10px] text-muted-foreground">{x.value.sender_id===uid?(es?"Tú":"You"):counterpart} · {new Date(x.value.created_at).toLocaleString(es?"es-CR":"en-US")}</p></div>:x.kind==="event"?<div key={`e${x.value.id}`} className="rounded-xl border border-border bg-secondary/40 p-3 text-xs"><strong>{es?"Actualización del pedido":"Order update"}</strong><p className="mt-1 text-muted-foreground">{x.value.message}</p><p className="mt-1 text-[10px] text-muted-foreground">{new Date(x.value.created_at).toLocaleString(es?"es-CR":"en-US")}</p></div>:<button key={`a${x.value.id}`} onClick={()=>void openAttachment(x.value)} className="flex w-full items-center gap-3 rounded-2xl border border-border p-3 text-left text-sm"><ImagePlus className="text-primary" size={20}/><span><strong>{x.value.attachment_type==="payment_proof"?(es?"Comprobante de pago":"Payment proof"):x.value.attachment_type==="shipment_proof"?(es?"Evidencia de envío":"Shipment proof"):(es?"Imagen adjunta":"Attachment")}</strong></span></button>)}</div>{notice&&<p className="mx-4 mb-2 rounded-xl bg-destructive/10 p-3 text-xs text-destructive">{notice}</p>}{!closed&&<div className="border-t border-border p-4"><textarea value={draft} onChange={e=>setDraft(e.target.value)} maxLength={2000} rows={3} placeholder={es?`Escribe un mensaje a ${counterpart}…`:`Write a message to ${counterpart}…`} className="w-full resize-none rounded-xl border border-border bg-background p-3 text-sm outline-none focus:border-primary"/><div className="mt-2 flex justify-between gap-2"><label className="inline-flex cursor-pointer items-center gap-2 rounded-xl border border-border px-3 py-2 text-xs font-semibold"><ImagePlus size={16}/>{uploading?(es?"Subiendo…":"Uploading…"):order.status==="payment_pending"&&isBuyer?(es?"Subir comprobante":"Upload payment proof"):order.status==="preparing_shipment"&&isSeller?(es?"Subir evidencia de envío":"Upload shipment proof"):(es?"Adjuntar imagen":"Attach image")}<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" disabled={uploading} onChange={e=>{void upload(e.target.files?.[0]||null);e.currentTarget.value=""}}/></label><button onClick={()=>void send()} disabled={busy||!draft.trim()} className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2 text-sm font-bold text-primary-foreground disabled:opacity-40"><Send size={16}/>{es?"Enviar":"Send"}</button></div></div>}</section></aside></div>
 {cancelOpen&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/60 p-4"><div className="w-full max-w-lg rounded-3xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="font-serif text-2xl">{es?`Cancelar pedido #${order.id}`:`Cancel order #${order.id}`}</h2><p className="mt-1 text-sm text-muted-foreground">{es?"Selecciona un motivo. Quedará registrado en el historial.":"Choose a reason. It will be recorded in the order history."}</p></div><button onClick={()=>setCancelOpen(false)}><X size={20}/></button></div><div className="mt-5 space-y-2">{cancellationOptions.map(r=><label key={r} className="flex cursor-pointer items-center gap-3 rounded-xl border border-border p-3"><input type="radio" name="reason" checked={cancelReason===r} onChange={()=>setCancelReason(r)}/><span className="text-sm">{reasonLabels[r][lang]}</span></label>)}</div>{order.response_required_from&&elapsedHours<24&&((isSeller&&order.response_required_from==="buyer")||(isBuyer&&order.response_required_from==="seller"))&&<p className="mt-3 text-xs text-muted-foreground">{es?`La opción por falta de respuesta se habilitará en aproximadamente ${Math.ceil(24-elapsedHours)} h.`:`No-response cancellation will unlock in about ${Math.ceil(24-elapsedHours)} h.`}</p>}<textarea value={cancelNote} onChange={e=>setCancelNote(e.target.value)} rows={3} maxLength={500} placeholder={es?"Comentario adicional (opcional)":"Additional note (optional)"} className="mt-4 w-full rounded-xl border border-border bg-background p-3 text-sm"/><div className="mt-4 flex gap-3"><button onClick={()=>setCancelOpen(false)} className="flex-1 rounded-xl border border-border px-4 py-3 font-bold">{es?"Volver":"Back"}</button><button onClick={()=>void cancelOrder()} disabled={!cancelReason||busy} className="flex-1 rounded-xl bg-destructive px-4 py-3 font-bold text-destructive-foreground disabled:opacity-40">{es?"Cancelar pedido":"Cancel order"}</button></div></div></div>}
 </main>}
