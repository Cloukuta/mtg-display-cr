"use client";
import {useEffect,useMemo,useState} from "react";
import {ArrowRight,Search,Store} from "lucide-react";
import AppHeader from "@/components/AppHeader";
import {getSupabase} from "@/lib/supabase";
import {DEFAULT_LANGUAGE,LANGUAGE_STORAGE_KEY,type Language} from "@/lib/i18n";

type Card={scryfall_id:string;name:string;image_uri:string|null;set_code:string|null;collector_number:string|null};
type Seller={id:string;public_name:string|null;slug:string|null};
type Item={id:number;seller_id:string;quantity:number;condition:string;language:string;finish:string;pricing_mode:"default"|"custom"|"discount";custom_price_crc:number|null;cards:Card|null};
type Price={scryfall_id:string;finish:string;condition:string;price_usd:number};type Settings={seller_id:string;usd_to_crc:number;discount_percent:number};
type Offer=Item&{seller:Seller;price:number|null};type Group={card:Card;offers:Offer[];sellers:number};
const crc=new Intl.NumberFormat("es-CR",{style:"currency",currency:"CRC",maximumFractionDigits:0});const RATE=520,DISCOUNT=20;
function nf(v:string){const f=(v||"").toLowerCase().replace(/[ _-]+/g,"");return f==="normal"?"nonfoil":f}
function price(i:Item,ps:Price[],cfg:Map<string,Settings>){if(i.pricing_mode==="custom")return i.custom_price_crc;const p=ps.find(x=>x.scryfall_id===i.cards?.scryfall_id&&nf(x.finish)===nf(i.finish)&&x.condition.toUpperCase()===i.condition.toUpperCase());if(!p)return null;const c=cfg.get(i.seller_id),base=Number(p.price_usd)*Number(c?.usd_to_crc??RATE);return Math.round(i.pricing_mode==="discount"?base*(1-Number(c?.discount_percent??DISCOUNT)/100):base)}
export default function GlobalCatalog(){const[lang,setLang]=useState<Language>(DEFAULT_LANGUAGE),[groups,setGroups]=useState<Group[]>([]),[loading,setLoading]=useState(true),[q,setQ]=useState("");const es=lang==="es";
 useEffect(()=>{const stored=localStorage.getItem(LANGUAGE_STORAGE_KEY);if(stored==="es"||stored==="en")setLang(stored);const h=(e:Event)=>{const n=(e as CustomEvent<Language>).detail;if(n==="es"||n==="en")setLang(n)};window.addEventListener("mtg-language-change",h);void(async()=>{const s=getSupabase();if(!s){setLoading(false);return}const{data:profiles}=await s.from("profiles").select("id,public_name,slug").eq("published",true).not("slug","is",null);const sellers=(profiles||[]) as Seller[],map=new Map(sellers.map(x=>[x.id,x]));const results=await Promise.all(sellers.map(x=>s.from("inventory_items").select("id,seller_id,quantity,condition,language,finish,pricing_mode,custom_price_crc,cards(scryfall_id,name,image_uri,set_code,collector_number)").eq("seller_id",x.id).eq("available",true).gt("quantity",0)));const items=results.flatMap(r=>(r.data||[]) as unknown as Item[]).filter(x=>x.cards);const ids=[...new Set(items.map(x=>x.cards!.scryfall_id))],sellerIds=[...new Set(items.map(x=>x.seller_id))];const[pr,cr]=await Promise.all([ids.length?s.from("card_prices").select("scryfall_id,finish,condition,price_usd").eq("source","cardkingdom").in("scryfall_id",ids):Promise.resolve({data:[]}),sellerIds.length?s.from("seller_pricing_settings").select("seller_id,usd_to_crc,discount_percent").in("seller_id",sellerIds):Promise.resolve({data:[]})]);const ps=(pr.data||[]) as Price[],cfg=new Map(((cr.data||[]) as Settings[]).map(x=>[x.seller_id,x]));const by=new Map<string,Offer[]>();for(const i of items){const seller=map.get(i.seller_id);if(!seller)continue;const offer={...i,seller,price:price(i,ps,cfg)};const key=i.cards!.scryfall_id;by.set(key,[...(by.get(key)||[]),offer])}setGroups([...by.values()].map(offers=>({card:offers[0].cards!,offers,sellers:new Set(offers.map(x=>x.seller_id)).size})).sort((a,b)=>a.card.name.localeCompare(b.card.name)));setLoading(false)})();return()=>window.removeEventListener("mtg-language-change",h)},[]);
 const filtered=useMemo(()=>{const x=q.trim().toLowerCase();return x?groups.filter(g=>`${g.card.name} ${g.card.set_code} ${g.card.collector_number}`.toLowerCase().includes(x)):groups},[groups,q]);
 return <main className="min-h-screen bg-background text-foreground"><AppHeader currentPath="/global-catalog"/><section className="border-b border-border bg-card/30"><div className="mx-auto max-w-7xl px-5 py-10 sm:px-7"><p className="text-xs font-black uppercase tracking-[.14em] text-primary">Marketplace</p><h1 className="mt-2 font-serif text-4xl font-bold">{es?"Catálogo global":"Global Catalog"}</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">{es?"Explora las impresiones disponibles en todas las vitrinas publicadas. Una carta aparece una sola vez aunque varios vendedores la tengan.":"Explore printings available across all published displays. Each card appears once even when multiple sellers have it."}</p><label className="relative mt-6 block max-w-xl"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder={es?"Buscar carta, set o collector…":"Search card, set, or collector…"} className="h-11 w-full rounded-xl border border-border bg-card pl-10 pr-4 text-sm outline-none focus:border-primary"/></label></div></section><section className="mx-auto max-w-7xl px-5 py-8 sm:px-7">{loading?<p className="text-sm text-muted-foreground">{es?"Cargando catálogo…":"Loading catalog…"}</p>:filtered.length?<><p className="mb-5 text-sm text-muted-foreground"><strong className="text-foreground">{filtered.length}</strong> {es?"impresiones disponibles":"available printings"}</p><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 sm:gap-5 lg:grid-cols-4 xl:grid-cols-5">{filtered.map(g=>{const only=g.sellers===1?g.offers[0]:null;const href=only?.seller.slug?`/v/${only.seller.slug}?card=${encodeURIComponent(g.card.scryfall_id)}`:`/search-vendor?card=${encodeURIComponent(g.card.scryfall_id)}`;return <a key={g.card.scryfall_id} href={href} className="group overflow-hidden rounded-2xl border border-border bg-card transition hover:border-primary/50"><div className="aspect-[488/680] overflow-hidden bg-secondary">{g.card.image_uri?<img src={g.card.image_uri} alt={g.card.name} className="h-full w-full object-cover transition group-hover:scale-[1.02]"/>:null}</div><div className="p-3"><h2 className="truncate font-semibold">{g.card.name}</h2><p className="mt-1 text-xs text-muted-foreground">{g.card.set_code?.toUpperCase()} #{g.card.collector_number}</p>{only?<><p className="mt-2 text-xs"><span className="font-bold text-primary">{(only.language||"en").toUpperCase()}</span> · {only.condition} · <span>{nf(only.finish)}</span></p><strong className="mt-2 block text-primary">{only.price==null?(es?"Precio pendiente":"Price pending"):crc.format(only.price)}</strong><span className="mt-3 inline-flex items-center gap-1 text-xs font-bold text-muted-foreground">{only.seller.public_name}<ArrowRight size={13}/></span></>:<><div className="mt-3 inline-flex items-center gap-2 rounded-full border border-border px-3 py-1.5 text-xs font-bold"><Store size={13} className="text-primary"/>{g.sellers} {es?"vendedores disponibles":"sellers available"}</div><span className="mt-3 flex items-center gap-1 text-xs font-bold text-primary">{es?"Comparar vendedores":"Compare sellers"}<ArrowRight size={13}/></span></>}</div></a>})}</div></>:<div className="rounded-2xl border border-border bg-card p-7 text-sm text-muted-foreground">{es?"Lamentablemente, ninguno de nuestros vendedores posee esta carta actualmente.":"Unfortunately, none of our sellers currently have this card."}</div>}</section></main>}
